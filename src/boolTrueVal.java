import java.util.Scanner;

        public class boolTrueVal {
            public static void main(String[] args) {
        System.out.println("Taking input from the user");
        Scanner sc = new Scanner(System.in);
        boolean b1 = sc.hasNextInt();
        //It shows true if the input value is true otherwise shows false
        System.out.println(b1);
        // String str1 = sc.next();
        // System.out.println(str1);
        // If we give a word as a input, it will print that,and if a line is given then the 1st word will only be printed
        //To display the whole line we can use:
        // String str2 = sc.nectLine();
            }
}
